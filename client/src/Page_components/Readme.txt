The folder Page_components is for all the individual page components.

A 'Page' is a visual collection of individual components. And example would be the home page. The home page is what the user sees and holds a collection of individual components (header, navbar, info , footer )

